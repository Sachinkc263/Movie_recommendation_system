# CineMatch - Hybrid Movie Recommendation System

A production-ready, full-stack movie recommendation system combining
**Collaborative Filtering** (SVD) and **Content-Based Filtering** (TF-IDF)
with a FastAPI backend, a React frontend, and Docker deployment.

---

## Features

- **Hybrid recommendations** - Centered TruncatedSVD + TF-IDF cosine similarity, blended at inference
- **Cold-start handling** - SVD fold-in, content profile, genre filter, popularity fallback (waterfall)
- **Implicit feedback** - likes, views, clicks and watches each carry calibrated weights
- **Smart search** - normalised matching (`spiderman` finds `Spider-Man`), debounced autocomplete, pagination
- **Poster freshness** - stale 2020 CSV poster paths refreshed on demand via TMDB API with server-side cache
- **Session identity** - anonymous UUID in localStorage, persists across browser sessions
- **One-command Docker** - `make start` builds and launches the full stack

---

## Recommendation Approaches

| Approach | When used | How it works |
|---|---|---|
| SVD fold-in (hybrid) | >= 3 interactions | Solves for virtual user vector; blends SVD + TF-IDF scores |
| TF-IDF content profile | Has liked >= 1 movie | Cosine similarity against liked movie feature vectors |
| Genre popularity | Genre prefs from onboarding | Popularity ranking filtered to preferred genres |
| Global popularity | Brand-new user | Bayesian-smoothed global popularity baseline |

---

## Dataset

**Source**: The Movies Dataset - Kaggle (rounakbanik)
https://www.kaggle.com/datasets/rounakbanik/the-movies-dataset

| File | Description |
|---|---|
| movies_metadata.csv | 45,466 movies - title, genres, overview, budget |
| ratings.csv | 26,024,289 ratings from 270,896 users |
| credits.csv | Cast and crew per movie |
| keywords.csv | Movie keywords and tags |
| links.csv | TMDB / IMDb ID cross-reference |

After cleaning and merging: **43,549 movies**, 100% TMDB ID coverage.

---

## Model Performance

Metrics fixed at K=10, relevance threshold >= 4.0, user-based 80/20 split (seed 42).

| Model | Precision@10 | Catalogue Coverage |
|---|---|---|
| Popularity baseline | 0.0412 | 0.012 |
| Collaborative (SVD only) | 0.0891 | 0.847 |
| Content-based (TF-IDF only) | 0.0734 | 0.923 |
| **Hybrid (SVD + TF-IDF)** | **0.1024** | **1.000** |

SVD validation RMSE: **0.8601** (n_components=50)

---

## Tech Stack

| Layer | Technology |
|---|---|
| Backend | Python 3.10, FastAPI, SQLAlchemy, SQLite |
| ML | scikit-learn (TruncatedSVD, TF-IDF), NumPy, SciPy |
| Frontend | React 18, Vite, Tailwind CSS, React Router |
| Serving | uvicorn (API), nginx (frontend + reverse proxy) |
| Containerisation | Docker, Docker Compose |

---

## Project Structure

    .
    |-- api/                         FastAPI backend
    |   |-- main.py                  App factory, health endpoint, startup hooks
    |   |-- models/                  SQLAlchemy ORM + Pydantic schemas
    |   |-- routes/                  movies, recommendations, users/interactions
    |   `-- services/
    |       |-- model_service.py     SVD + TF-IDF inference engine
    |       |-- user_service.py      Interaction aggregation, implicit weights
    |       `-- tmdb_service.py      TMDB poster cache
    |
    |-- frontend/                    React + Vite SPA
    |   |-- src/
    |   |   |-- api/client.js        All backend API calls
    |   |   |-- hooks/               useUser.js, usePoster.js
    |   |   |-- pages/               Home, Search, MovieDetail, Onboarding
    |   |   `-- components/          Navbar, MovieCard, MovieGrid, ...
    |   |-- nginx.conf               Reverse proxy + gzip + security headers
    |   `-- Dockerfile
    |
    |-- notebooks/                   Run in order 01 to 08
    |   |-- 01_eda.ipynb
    |   |-- 02_data_cleaning.ipynb
    |   |-- 03_data_preprocessing.ipynb
    |   |-- 04_feature_engineering.ipynb
    |   |-- 05_popularity_baseline.ipynb
    |   |-- 06_collaborative_filtering.ipynb
    |   |-- 07_content_based_filtering.ipynb
    |   `-- 08_hybrid_recommendation.ipynb
    |
    |-- scripts/
    |   |-- run_preprocessing.py     Step 1+2: clean raw data, create split
    |   |-- train_and_export.py      Step 3: train models, export artifacts
    |   `-- evaluate.py              Step 4: full offline evaluation
    |
    |-- src/                         ML library (data, models, evaluation, features)
    |-- data/
    |   |-- raw/                     Original CSVs (not committed - download from Kaggle)
    |   |-- processed/               Generated by scripts (not committed)
    |   |-- figures/                 EDA plots
    |   `-- cache/
    |       `-- poster_cache.json    TMDB poster path cache (committed)
    |
    |-- models/
    |   |-- artifacts/               Trained numpy/scipy files (not committed)
    |   `-- popularity_scores.csv    Bayesian popularity ranking
    |
    |-- docs/
    |   |-- SYSTEM_OVERVIEW.md       Plain-language overview for all audiences
    |   |-- ARCHITECTURE.md          Deep-dive technical documentation
    |   `-- PROJECT_IDEA.md          Original project brief
    |
    |-- reports/                     Offline evaluation metrics (JSON)
    |-- config/config.yaml           Hyperparameters for training + evaluation
    |-- Dockerfile                   Backend multi-stage build
    |-- docker-compose.yml           Orchestrates api + frontend services
    |-- Makefile                     Convenience targets (run: make help)
    |-- requirements.txt             Python dependencies
    `-- .env.example                 Environment variable template

---

## Installation

### Prerequisites

- Python 3.10+
- Node.js 20+ (for local frontend development)
- Docker + Docker Compose (for containerised deployment)
- A free TMDB API key: https://www.themoviedb.org/settings/api
- The Movies Dataset downloaded from Kaggle (link above)

### Environment Variables Setup

    cp .env.example .env

Edit `.env` and fill in:

    TMDB_API_KEY=your_key_here          # required for poster images
    COLD_START_THRESHOLD=3              # interactions before SVD fold-in
    DEBUG=false
    VITE_API_URL=http://localhost:8000  # for local dev (Docker uses /api)

---

## Running the Pipeline

Download the Kaggle dataset and place the CSV files in `data/raw/`.

    # Step 1+2: Clean raw data and create train/test split (~10-20 min)
    python scripts/run_preprocessing.py

    # Step 3: Train SVD + TF-IDF and export artifacts (~15 min)
    python scripts/train_and_export.py

    # Step 4 (optional): Run offline evaluation
    python scripts/evaluate.py

Artifacts are saved to `models/artifacts/` and `models/popularity_scores.csv`.

---

## Running the Backend

    # Install dependencies
    pip install -r requirements.txt

    # Start FastAPI with hot-reload
    uvicorn api.main:app --reload --port 8000

    # Or use make
    make setup
    make dev-api

API available at http://localhost:8000
Interactive docs at http://localhost:8000/docs

---

## Running the Frontend

    cd frontend
    npm install
    npm run dev

    # Or use make from project root
    make dev-frontend

Frontend available at http://localhost:5173 (points to API on port 8000)

---

## Docker Usage

    # Build and start all services
    make start

    # Equivalent without make
    docker compose up --build -d

Services:
- App:      http://localhost:3000
- API:      http://localhost:8000
- API docs: http://localhost:8000/docs

    make stop       # stop all containers
    make restart    # rebuild and restart
    make logs       # tail live logs

The `data/` and `models/` directories are mounted as volumes so no rebuild
is needed when artifacts or the database change.

In Docker, the frontend build bakes in `VITE_API_URL=/api`, and nginx
reverse-proxies `/api/*` to the backend container, so the browser never
needs to know the backend port.

---

## API Reference

Base URL: `http://localhost:8000`

| Method | Endpoint | Description |
|---|---|---|
| GET | /health | Health check |
| POST | /users/ | Create anonymous session user |
| GET | /users/{id} | Get user profile |
| PUT | /users/{id}/preferences | Save genre preferences |
| POST | /interactions/ | Log interaction (like/view/click/search) |
| GET | /interactions/{id} | Get user history |
| GET | /movies/popular | Popular movies |
| GET | /movies/search?q=&n= | Full-text search |
| GET | /movies/suggestions?q=&n= | Autocomplete suggestions |
| GET | /movies/{id} | Movie detail |
| GET | /movies/{id}/similar | Content-similar movies |
| GET | /movies/{id}/poster-url | Fresh TMDB poster URL |
| GET | /recommendations/{user_id} | Personalised hybrid recommendations |
| GET | /recommendations/because-you-watched/{movie_id} | Item-based recommendations |

---

## Notebooks

Run in order - each saves outputs consumed by the next.

| Notebook | Purpose |
|---|---|
| 01_eda.ipynb | Explore rating distributions, genre breakdown, sparsity, temporal trends |
| 02_data_cleaning.ipynb | Fix types, parse JSON columns, remove duplicates, save *_clean.csv |
| 03_data_preprocessing.ipynb | Merge datasets, normalise features, user-based train/test split |
| 04_feature_engineering.ipynb | Engineer movie and user features for model training |
| 05_popularity_baseline.ipynb | Bayesian popularity baseline - sets the performance floor |
| 06_collaborative_filtering.ipynb | Centered TruncatedSVD - grid-search n_components, evaluate RMSE + P@10 |
| 07_content_based_filtering.ipynb | TF-IDF on feature soup - genres + overview + cast + keywords |
| 08_hybrid_recommendation.ipynb | Hybrid combination, final metrics, artifact export |

---

## Evaluation Summary

All metrics evaluated offline with a fixed contract (K=10, threshold=4.0, seed=42)
to ensure fair comparison across models.

Run `python scripts/evaluate.py` to reproduce all metrics.
Results are saved to `reports/` as JSON files.

Key findings:
- SVD achieves strong precision but misses niche movies (84.7% coverage)
- TF-IDF reaches 92.3% coverage but lower precision on popular titles
- The hybrid combines both strengths: highest precision AND 100% coverage
- SVD fold-in allows immediate personalisation for new users without retraining

---

## Makefile Quick Reference

    make help           Show all available targets
    make setup          Install Python dependencies
    make train          Run full preprocessing + training pipeline
    make evaluate       Run offline evaluation suite
    make dev-api        Start FastAPI with hot-reload (port 8000)
    make dev-frontend   Start Vite dev server (port 5173)
    make start          Docker: build + start all services
    make stop           Docker: stop all services
    make logs           Docker: tail live logs
    make clean          Remove __pycache__ and build artifacts

---

## License

MIT
